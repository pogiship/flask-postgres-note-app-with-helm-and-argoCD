apiVersion: v1
kind: Secret
metadata:
  namespace: {{ .Values.app.namespace }}
  name: {{ .Values.app.db.name }}-secret
  labels:
    app: {{ .Values.app.name }}

data:
  POSTGRES_PASSWORD: {{ .Values.app.db.password | b64enc }}
  #POSTGRES_PASSWORD: cG9zdGdyZXM=
